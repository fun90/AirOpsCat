## 1. SSH 配置统一

- [x] 1.1 搜索并列出所有 `SshConfig` 构造点，确认连接测试、部署、配置同步、维护、监控采集和在线连接查询入口。
- [x] 1.2 新增统一的服务器 SSH 配置构造组件，支持 `Server` 和必要的 `ServerDto` 输入。
- [x] 1.3 在统一构造组件中处理默认端口、默认用户名、超时时间、`PASSWORD` 密码认证和 `KEY` 私钥内容认证。
- [x] 1.4 为统一构造组件补充单元测试，覆盖密码、密钥、默认端口、默认用户名和空认证内容场景。

## 2. 远程操作接入

- [x] 2.1 将节点部署中的 `SshConfig` 构造逻辑替换为统一组件。
- [x] 2.2 将服务器配置同步中的 `SshConfig` 构造逻辑替换为统一组件。
- [x] 2.3 将服务器维护中的 `SshConfig` 构造逻辑替换为统一组件。
- [x] 2.4 将监控采集和调度辅助中的 `SshConfig` 构造逻辑替换为统一组件。
- [x] 2.5 将在线连接查询或其他 SSH 入口中的 `SshConfig` 构造逻辑替换为统一组件。

## 3. 真实连接测试

- [x] 3.1 将服务器连接测试服务从模拟成功改为创建 SSH 连接并执行轻量远程命令。
- [x] 3.2 调整连接测试接口响应，返回成功或失败信息，并确保不泄露密码、私钥内容或认证秘密。
- [x] 3.3 为连接测试失败路径补充可诊断日志，日志不得包含认证内容。
- [x] 3.4 补充连接测试行为测试，覆盖成功、认证失败和命令执行失败的服务逻辑。

## 4. 前端与校验

- [x] 4.1 调整服务器新增和编辑表单文案，明确密钥输入为未加密私钥内容。
- [x] 4.2 在前端或后端校验中明确不支持带密码短语的私钥，并给出清晰错误信息。
- [x] 4.3 检查认证内容显示、日志和错误提示，避免密码或私钥内容被明文泄露。

## 5. Native 支持与验证

- [x] 5.1 检查现有 `META-INF/native-image` 配置是否覆盖 JSch 密钥登录所需资源和安全服务。
- [x] 5.2 如 Native 构建或运行缺少算法、资源或安全 provider，补充对应 native-image 配置。
- [x] 5.3 使用本地可用私钥对脱敏测试主机（例如 `root@104.194.x.x`）执行 JVM 模式密钥连接测试，验证真实连接测试和至少一个只读 SSH 远程命令。
- [ ] 5.4 执行 Native 构建，启动 Native 产物，并使用本地可用私钥对脱敏测试主机（例如 `root@104.194.x.x`）验证密钥连接测试和至少一个只读 SSH 远程命令。
- [x] 5.5 确认测试命令、日志、文档和提交内容均不包含私钥内容或本地私钥路径。

## 6. 收尾

- [x] 6.1 检查新增或修改的 DTO、模板参数和 JSON 序列化类型是否需要登记到 `JsonReflectionConfiguration`。
- [x] 6.2 运行相关单元测试和可行的构建检查。
- [x] 6.3 运行 `openspec validate support-server-ssh-key-auth --strict` 并修复所有问题。
