## 1. 状态模型

- [x] 1.1 新增节点部署状态枚举或集中常量，定义待部署、已部署、待删除三态及标签文案。
- [x] 1.2 将 `Node`、`NodeDto` 的部署状态说明从“未部署 / 已部署”更新为三态语义。
- [x] 1.3 替换后端服务和仓库中散落的 `deployed = 0/1` 判断，使用枚举或辅助方法表达业务含义。

## 2. 节点生命周期

- [x] 2.1 调整新增、编辑、启用/禁用、批量标签调整、节点组联动、版本还原等流程，将需要刷新远端配置的节点标记为待部署。
- [x] 2.2 调整删除节点流程：待部署节点直接删除，已部署节点标记待删除，待删除节点保持幂等。
- [x] 2.3 补齐物理删除前的关联清理，覆盖节点标签关系、当前部署版本记录及其他直接依赖。

## 3. 部署流程

- [x] 3.1 调整普通部署查询，使其处理待部署和待删除节点；指定节点部署也遵循同样状态筛选。
- [x] 3.2 调整部署数据加载或配置构建逻辑，使待删除节点触发服务器刷新但不进入新配置。
- [x] 3.3 调整部署成功持久化：普通节点置为已部署并记录版本，待删除节点在成功刷新后物理删除。
- [x] 3.4 确保部署失败时状态保持不变，方便管理员重试。

## 4. 订阅与查询

- [x] 4.1 确认订阅节点只读取已部署、未禁用、代理类型节点，排除待部署和待删除。
- [x] 4.2 为节点列表增加三态筛选参数，保留旧 `deployed` 布尔参数兼容现有调用。
- [x] 4.3 更新节点统计或筛选摘要，使待删除状态不会被误显示为未部署。

## 5. 管理端前端

- [x] 5.1 更新节点列表部署状态筛选，展示“全部 / 待部署 / 待删除 / 已部署”。
- [x] 5.2 更新状态徽章样式和文案，部署中仍作为临时前端状态覆盖显示。
- [x] 5.3 调整操作按钮：待部署显示部署，已部署显示重新部署，待删除显示部署/刷新删除或禁用其他编辑操作。
- [x] 5.4 更新删除确认弹窗文案，区分直接删除和标记待删除。

## 6. 验证

- [x] 6.1 添加或更新后端单元测试，覆盖状态转换、删除分支、部署成功后清理和失败保持状态。
- [x] 6.2 运行窄范围 Maven 测试，至少覆盖节点服务和部署服务相关测试。
- [ ] 6.3 手动验证控制台：新增节点、修改已部署节点、删除已部署节点、批量部署后物理删除、订阅节点列表排除待处理状态。
