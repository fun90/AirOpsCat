## 1. 数据模型与仓库

- [x] 1.1 新增 `AccountNodeSubscriptionDomainBinding` 实体，字段包含账户 ID、节点 ID、DNS 记录 ID、启用状态、备注、创建时间和更新时间
- [x] 1.2 新增数据库迁移或等价 DDL，创建 `account_node_subscription_domain_binding` 表
- [x] 1.3 为 `account_id + node_id` 添加唯一约束，保证同一账户同一节点最多一条绑定
- [x] 1.4 新增 `AccountNodeSubscriptionDomainBindingRepository`，支持按账户、按节点、按账户与节点集合查询绑定

## 2. DTO 与服务层

- [x] 2.1 新增绑定请求 DTO 和响应 DTO，包含账户、节点、DNS 记录、完整域名、启用状态和展示字段
- [x] 2.2 在 `JsonReflectionConfiguration` 注册新增请求/响应 DTO
- [x] 2.3 新增绑定服务，封装查询、创建或更新、启用、禁用和删除逻辑
- [x] 2.4 保存绑定时校验账户、节点和 DNS 记录存在且 `full_name` 有效，并将唯一约束冲突转换为可读错误
- [x] 2.5 批量查询账户订阅域名覆盖时，仅返回启用且 DNS 记录完整域名有效的绑定

## 3. 管理 API

- [x] 3.1 新增后台控制器，提供按账户查询绑定列表接口
- [x] 3.2 新增按节点查询绑定列表接口
- [x] 3.3 新增创建或更新账户节点订阅域名绑定接口
- [x] 3.4 新增启用、禁用和删除绑定接口
- [x] 3.5 确认所有接口错误返回与现有后台 API 风格一致
- [x] 3.6 新增按 `domain_dns_record.full_name` 搜索 DNS 记录的后台接口，供绑定表单选择
- [x] 3.7 新增按账户查询可绑定节点的后台接口，默认返回前 20 条并限定为账户标签授权节点

## 4. 订阅生成集成

- [x] 4.1 调整 `SubscriptionService.loadAvailableSubscriptionNodes`，在账户可用节点确定后批量加载账户节点订阅域名绑定
- [x] 4.2 对存在启用绑定且 DNS 记录完整域名有效的节点，将订阅用 `NodeDto.serverHost` 覆盖为绑定 `full_name`
- [x] 4.3 保持绑定缺失、禁用或域名不可用时继续使用现有节点地址解析逻辑
- [x] 4.4 确认绑定不会把未通过标签授权的节点加入账户订阅
- [x] 4.5 确认 `DeploymentDataLoader`、节点部署快照和节点用户列表生成不读取该绑定

## 5. 前端管理入口

- [x] 5.1 在账户详情或账户编辑相关界面增加节点订阅域名绑定查看入口
- [x] 5.2 复用现有远程搜索组件选择账户可用节点和 DNS 记录完整域名，节点默认加载 20 条
- [x] 5.3 支持创建或更新某账户在某节点上的绑定 DNS 记录
- [x] 5.4 支持启用、禁用和删除绑定，并在操作后刷新绑定列表
- [x] 5.5 在界面文案中明确该域名只影响订阅连接地址，不触发节点部署

## 6. 验证

- [x] 6.1 验证同一账户同一节点重复保存时更新既有绑定，不产生重复记录
- [x] 6.2 验证同一账户可为多个节点绑定不同 DNS 记录，同一节点可为多个账户绑定不同 DNS 完整域名
- [x] 6.3 验证订阅生成对启用绑定使用绑定 `full_name`，对禁用或无效绑定回退默认节点地址
- [x] 6.4 验证绑定不会改变标签授权得到的可用节点集合
- [x] 6.5 验证绑定创建、更新、禁用和删除不会标记节点待部署
- [x] 6.6 验证节点选择只出现账户可用节点，且默认加载前 20 条
- [x] 6.7 运行项目测试或执行等价手动验证，覆盖新增 API、订阅生成和前端主要操作
- [x] 6.8 运行 `openspec validate add-account-node-subscription-domain-binding --strict`
