## 1. 数据模型与迁移

- [x] 1.1 为 `AccountOnlineIp` 增加节点区分字段，至少包含 `nodeId` 和 `nodeTag`，并让 DTO 暴露节点名称或节点标识
- [x] 1.2 为 `AccountOnlineIp` 增加 `connectionId`，并调整 `account_online_ip` 唯一约束，使同一账户、客户端 IP、服务器 IP、节点下的多条连接可以保留独立记录
- [x] 1.3 将账户限制字段从 `maxOnlineIps` 迁移为 `maxConnections`，同步实体、DTO、请求对象、页面字段和历史数据初始值
- [x] 1.4 全局清理 `maxOnlineIps`、`最大在线 IP 数`、`最大在线IP数` 的应用层引用，确保业务代码和前端不再读写旧字段
- [x] 1.5 新增通用告警状态实体与仓库，字段覆盖告警类型、资源、指纹、状态、阈值、当前值、触发时间、通知时间、恢复时间和摘要
- [x] 1.6 准备必要的数据库迁移说明或 SQL，覆盖账户最大连接数、新增在线记录字段、新唯一约束和通用告警状态表
- [x] 1.7 检查并更新 `JsonReflectionConfiguration` 中新增或变更的 DTO、实体、响应类型注册

## 2. 在线采集与节点映射

- [x] 2.1 扩展 `SingBoxConnectionMetadata`，兼容读取连接 metadata 中的入站标识字段
- [x] 2.2 在单台服务器刷新在线连接时加载服务器下已部署、未禁用的代理节点，并建立 `node_<id>` 到节点的映射
- [x] 2.3 调整 `AccountOnlineIpService.refreshFromConnections` 入参和写入逻辑，保存连接 ID、服务器 IP、节点 ID、节点 tag
- [x] 2.4 将同一轮采集去重口径调整为优先按账户、连接 ID、服务器 IP 去重，连接 ID 缺失时按连接关键字段退化去重
- [x] 2.5 增加按节点 ID 查询当前有效在线记录、按节点 ID 批量统计在线数量的仓库和服务方法

## 3. 超限告警

- [x] 3.1 新增 `AccountOnlineLimitAlertService`，按当前有效窗口聚合账户在线记录并计算在线连接数
- [x] 3.2 仅对 `maxConnections` 大于 0 的账户执行超限判断，并生成包含账户、限制值、当前连接数、去重客户端 IP 数和节点摘要的告警内容
- [x] 3.3 将账户连接数超限状态写入通用告警状态表，支持 ACTIVE、RECOVERED、首次触发、最近触发、最近通知、恢复时间和触发次数
- [x] 3.4 通过现有 `BarkService` 发送超限告警，并按通用告警状态的最近通知时间控制重复通知
- [x] 3.5 在系统配置中心新增连接数超限告警开关、最小通知间隔和可选恢复通知配置
- [x] 3.6 在在线刷新任务完成后触发超限检查，并确保单个账户告警失败不影响其他账户检查
- [x] 3.7 保持服务器流量、服务器负载等现有告警逻辑不变，本期不接入通用告警状态表

## 4. 后端接口

- [x] 4.1 新增或扩展节点管理接口，支持 `GET /api/admin/nodes/{id}/online-accounts`
- [x] 4.2 在节点分页 DTO 或现有节点 DTO 中补充当前在线连接数
- [x] 4.3 在账户分页 DTO 或现有账户 DTO 中补充当前在线连接数
- [x] 4.4 调整账户在线连接查询接口返回内容，包含连接 ID、服务器 IP 与节点区分信息
- [x] 4.5 保持服务器维度在线账户接口兼容，继续按服务器 IP 返回当前有效在线记录

## 5. 前端展示

- [x] 5.1 将账户表单和详情中的“最大在线 IP 数”改为“最大连接数”
- [x] 5.2 在账户列表和账户详情中展示当前在线连接数，并提供查看在线连接详情入口
- [x] 5.3 在账户在线连接详情弹窗中增加连接 ID、节点列或节点标识展示
- [x] 5.4 在节点列表或节点详情中展示当前在线连接数和查看入口
- [x] 5.5 复用现有在线账户详情弹窗模式，支持查看指定节点当前在线连接
- [x] 5.6 处理节点信息缺失时的空状态展示，避免前端渲染异常

## 6. 验证

- [x] 6.1 为在线记录写入、节点映射、连接数超限计数、通用告警状态更新和最小通知间隔补充单元测试或服务级测试
- [x] 6.2 手动验证同一账户同一客户端 IP 建立多条连接时，账户详情展示多条连接记录，且超限计数按连接数累计
- [x] 6.3 手动验证账户连接数超限告警开关、最小通知间隔、恢复状态和发送失败不阻塞刷新任务
- [x] 6.4 执行 `./mvnw test` 或至少执行相关测试，并记录无法执行的原因
