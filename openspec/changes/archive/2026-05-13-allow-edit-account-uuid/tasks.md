## 1. 前端编辑表单

- [x] 1.1 移除 `src/main/resources/templates/person/account/modals.html` 中编辑账户 UUID 输入框的只读限制，保留现有 `v-model` 和重新生成按钮
- [x] 1.2 在账户编辑表单校验中增加 UUID 必填和格式校验，并将错误绑定到 UUID 输入框展示
- [x] 1.3 确认 `prepareUpdateData()` 提交编辑后的 UUID，保存成功后列表和详情使用接口返回值刷新

## 2. 后端更新校验

- [x] 2.1 在账户仓储或服务中增加按 UUID 查询其他账户是否存在的能力
- [x] 2.2 在 `AccountService.updateAccount()` 中对提交 UUID 执行 trim、非空校验、标准 UUID 格式校验和排除当前账户的唯一性校验
- [x] 2.3 保存 UUID 的标准字符串值，并确保 UUID 修改后账户搜索、订阅生成和节点部署继续读取账户当前字段
- [x] 2.4 让非法 UUID、空 UUID、重复 UUID 的更新请求返回 400，并包含前端可展示的错误消息

## 3. 验证

- [ ] 3.1 手动验证编辑弹窗可直接输入 UUID、可重新生成后保存，并能在账户列表和详情看到新值
- [ ] 3.2 手动验证空 UUID、非法 UUID、其他账户重复 UUID 均无法保存且有明确错误提示
- [x] 3.3 运行可用的后端测试或最小构建检查，确认账户更新链路无编译错误
