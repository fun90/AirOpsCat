## 1. 后端请求契约与事务编排

- [x] 1.1 新增开户用户资料、组合创建请求和组合创建响应 DTO，明确已有用户与新用户两种互斥来源，接收可选首期金额与付款方式，并返回 `UserDto`、`AccountDto` 与可空的 `TransactionDto`
- [x] 1.2 在用户服务中补齐可复用的新用户字段规范化、邮箱唯一性、密码长度与密码加密处理，并保持现有用户创建行为兼容
- [x] 1.3 抽取开户与续期可复用的账户有效期描述或月份计算逻辑，保持两种交易流水的期限描述一致
- [x] 1.4 新增事务编排服务，在单一事务内完成新用户或已有用户解析、账户创建、标签关联及可选首期收入交易创建，确保任一步骤异常时整体回滚
- [x] 1.5 在账户管理控制器新增组合创建端点，将用户来源、字段校验、重复邮箱、金额/付款方式及不存在资源错误映射为明确的 400/409 响应
- [x] 1.6 将新增请求/响应 DTO 注册到 `JsonReflectionConfiguration`，检查 JVM 与原生镜像 JSON 序列化要求

## 2. 账户新增界面

- [x] 2.1 调整账户新增弹窗，加入“创建新用户/选择已有用户”模式切换及新用户邮箱、密码、昵称、角色等字段，并保留现有用户搜索组件
- [x] 2.2 在账户到期时间附近增加首期金额和付款方式字段，复用续期弹窗的付款方式选项与“留空不生成流水”提示
- [x] 2.3 在 `person/account.js` 建立单一新增表单默认值工厂，设置最大连接数 100、最大 IP / 设备数 3、流量限制 500 GB、下行限速 20 Mbps、上行限速 10 Mbps、级别 4、节点倍数 2，并将首期金额和付款方式初始化为空
- [x] 2.4 让初次初始化、打开新增弹窗和成功创建后的表单重置均使用默认值工厂，同时确保账户编辑表单继续回显已保存值
- [x] 2.5 按当前用户模式实现前端校验和组合请求构造，校验正金额必须选择付款方式、避免提交隐藏模式字段，并在失败时保留表单和展示后端错误
- [x] 2.6 创建成功后关闭弹窗、清理搜索组件和校验状态，并刷新账户列表与统计数据；响应含交易时提示首期流水已记录

## 3. 自动化验证

- [x] 3.1 增加组合创建服务测试，覆盖新用户与账户成功创建、已有用户新增账户、自动生成账户标识、七项参数持久化及首期收入交易字段
- [x] 3.2 增加事务回滚测试，覆盖重复邮箱、非法账户资料、不存在的已有用户、无效标签和交易保存失败，确认不会遗留用户、账户、标签关系或流水
- [x] 3.3 增加控制器请求校验测试，覆盖用户来源同时存在或同时缺失、密码过短、金额非法、缺少或非法付款方式及结构化错误响应
- [x] 3.4 增加开户与续期交易描述测试，确认业务关联、收入类型、期限描述、账户备注及未填写金额不生成流水的行为一致
- [x] 3.5 增加前端默认值、首期交易校验与重置逻辑测试；若项目无前端测试框架，则以可独立调用的函数断言或浏览器手动验证记录替代

## 4. 集成与回归验证

- [x] 4.1 运行 Maven 编译和相关测试，确认新增 DTO、事务服务及控制器通过 JVM 构建
- [x] 4.2 在开发模式下验证新用户一站式开户、已有用户新增账户、首期交易生成、金额留空不生成流水、失败后修正重试及成功后列表刷新
- [x] 4.3 回归现有用户独立创建、账户独立创建和账户编辑流程，确认兼容性及编辑值不被新增默认值覆盖
- [x] 4.4 检查组合创建后账户的订阅、部署及限流同步仍读取实际持久化值，并确认交易列表可按新账户查看首期流水，记录验证结果

## 验证记录

- Maven 完整测试共 58 项通过；本变更定向 Java 测试共 16 项通过；前端 Node 测试共 3 项通过。
- Quarkus 开发模式可正常启动并监听 8080 端口；浏览器控制层在登录跳转时持续超时，因此未向本地数据库写入手工验收数据，界面流程改由模板静态核对、前端纯函数测试及控制器/服务测试交叉验证。
- 账户编辑仍使用独立的 `editedItem` 与 `prepareEditForm`，新增默认值工厂仅用于新增表单初始化和重置。
- `SubscriptionService`、`DeploymentDataLoader`、`AccountGuardAggregator`、`AccountTrafficLimitService` 与 `AccountTrafficStatsService` 均继续从持久化的 `Account` 字段读取节点倍数、连接/IP、速度和流量限制；首期流水使用 `businessTable=account` 与新账户 ID 关联，沿用现有交易查询模型。

## 5. 开户表单字段精简

- [x] 5.1 移除新增弹窗中的用户备注名和用户备注输入项，并停止前端组合请求提交这两个隐藏字段
- [x] 5.2 将账号备注名移动到账号输入框之后，增加非空前端校验与字段错误提示
- [x] 5.3 更新前端纯函数测试并运行 OpenSpec、JavaScript 语法和定向测试验证

## 6. 移动端账户操作面板

- [x] 6.1 移动端将账户操作入口切换为表格外独立面板，桌面端保留原下拉菜单
- [x] 6.2 复用编辑、配置链接、续期、重置认证码、部署、启禁用和删除处理逻辑，并确保面板关闭后再执行
- [x] 6.3 增加静态结构检查并运行 JavaScript 语法、OpenSpec 严格校验和相关回归测试
