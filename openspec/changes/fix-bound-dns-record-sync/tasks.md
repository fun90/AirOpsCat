## 1. DNS 同步保护

- [x] 1.1 将 DNS 拉取同步从全量删除重建改为增量同步，远端仍存在的记录应复用本地记录 ID
- [x] 1.2 处理本地存在但远端不存在的记录：未绑定则删除，已绑定则保留并标记为 `PENDING_DELETE`
- [x] 1.3 当同步保留了待删除绑定记录时，将域名同步状态设为 `NOT_SYNCED` 并返回明确提示

## 2. 删除与解绑保护

- [x] 2.1 为账户节点订阅域名绑定仓储新增按 DNS 记录 ID 判断是否存在绑定的方法
- [x] 2.2 手动删除 DNS 记录时，如果存在绑定则拒绝删除并提示先解绑
- [x] 2.3 切换 DNS 服务商绑定或解绑域名 DNS 前，如果域名下存在已绑定记录则拒绝操作并提示先解绑

## 3. 验证

- [x] 3.1 执行 `./mvnw -DskipTests compile` 验证编译通过
