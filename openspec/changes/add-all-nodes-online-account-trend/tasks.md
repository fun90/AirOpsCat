## 1. 后端数据契约

- [x] 1.1 设计所有节点趋势总览 DTO，包含摘要、每日聚合点位和节点排行或对比项。
- [x] 1.2 在 `JsonReflectionConfiguration` 登记新增 DTO，覆盖控制器响应和 JSON 序列化场景。
- [x] 1.3 扩展 `NodeOnlineAccountDailyStatsRepository`，提供按日期范围查询全部节点统计和必要的聚合查询。

## 2. 总览统计服务与 API

- [x] 2.1 扩展 `NodeOnlineAccountStatsService`，实现所有节点总览摘要计算，包括节点总数、有数据节点数、周期峰值和周期平均值。
- [x] 2.2 实现所有节点每日聚合趋势数据，按日期返回全节点每日在线账户数和全节点峰值在线账户数。
- [x] 2.3 实现节点使用排行或对比数据，突出高峰节点和低利用率节点，并提供单节点跳转所需节点信息。
- [x] 2.4 扩展 `NodeOnlineAccountStatsController`，新增所有节点总览摘要和图表数据接口，并限制安全天数范围。
- [x] 2.5 确保查询范围无统计数据时 API 返回可渲染的空摘要和空点位，而不是错误响应。

## 3. 控制台总览页面

- [x] 3.1 在 `ConsolePageRegistry` 注册所有节点在线趋势总览页面，使用不在侧边菜单展示或符合节点管理工作流的展示方式。
- [x] 3.2 新增所有节点趋势总览 Qute 模板，包含摘要卡、范围切换、刷新、返回节点列表和节点对比区域。
- [x] 3.3 新增所有节点趋势总览 JavaScript，复用 ApexCharts 主题适配、加载状态、空状态和范围切换交互。
- [x] 3.4 在节点对比区域提供跳转单节点趋势页面的入口。

## 4. 节点列表入口

- [x] 4.1 在节点列表页面工具区或合适操作区域增加“全部在线趋势”入口。
- [x] 4.2 验证新增入口不影响节点列表现有筛选、表格、移动端布局和单节点趋势入口。

## 5. 验证

- [x] 5.1 运行 OpenSpec 校验，确认 proposal、design、specs 和 tasks 均可被识别。
- [x] 5.2 编译项目，修复新增 DTO、服务、控制器、模板和脚本引入的问题。
- [x] 5.3 手动验证所有节点趋势总览空数据状态、有数据状态和 7/30/90 天范围切换。
- [x] 5.4 手动验证节点列表总览入口以及总览页跳转单节点趋势页。
- [x] 5.5 验证同一账号跨多个节点时，总览页按节点承载账户数聚合展示，并且文案不将其误表述为全站唯一账号数。
